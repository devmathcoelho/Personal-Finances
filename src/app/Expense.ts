export type Expense = {
    name: string,
    amount: number,
    category: string,
    date: string
}

export type Revenue = {
    name: string,
    amount: number,
    category: string,
    date: string
}